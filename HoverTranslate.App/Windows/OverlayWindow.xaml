<Window x:Class="HoverTranslate.App.Windows.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="HoverTranslate"
        Width="420" Height="220"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize">
  <Border Background="#F0222222" CornerRadius="8" Padding="14" BorderBrush="#55FFFFFF" BorderThickness="1">
    <Grid>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
        <RowDefinition Height="*"/>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <TextBlock x:Name="TitleText" Text="译文" Foreground="#EEEEEE" FontSize="14" FontWeight="SemiBold"/>
      <TextBox x:Name="ResultText" Grid.Row="1" Margin="0,10,0,10"
               TextWrapping="Wrap" AcceptsReturn="True" IsReadOnly="True"
               Background="#33000000" Foreground="#FFFFFF" BorderThickness="0"
               VerticalScrollBarVisibility="Auto" FontSize="13"/>
      <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
        <Button Content="复制" Width="64" Margin="0,0,8,0" Click="OnCopy"/>
        <Button Content="关闭" Width="64" Click="OnClose"/>
      </StackPanel>
    </Grid>
  </Border>
</Window>
